NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_API_BASE_URL=

# Local development only. Reveals a button that skips the teaching assessment.
# The server enforces this independently and ignores the button unless
# DEV_ALLOW_CERTIFICATION_BYPASS is also set on the backend.
NEXT_PUBLIC_DEV_CERTIFICATION_BYPASS=false
